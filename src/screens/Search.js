import React from "react";
import {StyleSheet, Text} from "react-native";

export const Search = () => <Text style={styles.text}>Search screen</Text>

const styles = StyleSheet.create({
  text: {
    textTransform: 'uppercase'
  }
})