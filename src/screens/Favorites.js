import React from "react";
import {StyleSheet, Text} from "react-native";

export const Favorites = () => <Text style={styles.text}>Favorites screen</Text>

const styles = StyleSheet.create({
  text: {
    textTransform: 'uppercase'
  }
})