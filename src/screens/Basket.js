import React from "react";
import {StyleSheet, Text} from "react-native";

export const Basket = () => <Text style={styles.text}>Basket screen</Text>

const styles = StyleSheet.create({
  text: {
    textTransform: 'uppercase'
  }
})